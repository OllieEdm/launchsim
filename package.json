{
  "name": "launchsim",
  "version": "1.0.0",
  "description": "2D rocket launch simulator with a browser UI",
  "private": true,
  "scripts": {
    "test": "node --test",
    "shots": "node scripts/screenshot.mjs"
  },
  "devDependencies": {
    "playwright": "^1.61.0"
  }
}
